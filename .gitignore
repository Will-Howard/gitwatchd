# Build artifacts (hand-assembled .app bundle + compiled binary)
build/

# macOS
.DS_Store
CLAUDE.local.md

# Local end-to-end fiddling repos (devbox remote); never part of this repo
test-repos/

# Code-signing material (private key + CSR); never part of this repo
signing/
